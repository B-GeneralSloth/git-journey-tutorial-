# "open ." commando werkt niet op windows, hiervoor gebruik je "explorer ."
# cd wordt gebruikt om directories te openen ("cd .." om terug te gaan)
# mkdir wordt gebruikt om directories te maken
# touch wordt gebruikt om files aan te maken (one.txt, two.txt...)
# met git status kan je zien welke files veranderd zijn
      # als hier untracked files bij staan dan zijn die nog niet toegevoegd aan de repository
# als je iets wil deleten en dan gelijk stagen gebruik commando "git rm four.txt" (four.txt als voorbeeld)
# voor echte reset (deleted files terugkrijgen bijvoorbeeld) gebruik "git reset --hard
# voor geschiedenis bekijken en commit strings gebruik "git log", dit staat vol met commit-id's
# voor username of email veranderen gebruik git config --global user.name/user.email "x"
# het verschil tussen twee verschillende commits kan je zien met "git diff 'commit-id1 commit-id2' "

# Committen
(Staging)
# met git add --all kunnen de files naar de stage worden gebracht
# met git add . kan je alle files van een folder naar stage brengen als je je in de folder bevind
# met git add * stage je alle files behalve de verwijderde files
# met git add * kan je ook specifieke files toevoegen (bijv. git add *.txt)

# een commit moet gemaakt worden als je wil dat de verandering effect heeft
# om te comitten gebruik commando "git commit -m"
# om de commit te ontdoen gebruik commando "git reset HEAD~"
# -m achter de commit commando is voor een berichtje achterlaten
# je kan terug gaan naar een vorige commit door de geschiednis te bekijken met behulp van git log
# en vervolgens "git checkout 'commit id' " te gebruiken

# Branchen
# gebruik git branch om alle branches te bekijken en nieuwe branches te maken ("git branch 'naam' ")
# gebruik "git checkout 'naam branch' " om te switchen naar een andere branch
# als er een file wordt gecommit in een andere branch dan zal deze file niet meer te zien zijn in de main branch

# Mergen
# voor mergen ga je eerst naar de branch waarmee je wilt dat gemerged wordt, vervolgens gebruik je "git merge (branch naam) -m"
# een merge moet je twee keer uitvoeren wil je dat beide branches hetzelfde zijn
